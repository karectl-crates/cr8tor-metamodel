---
id: https://w3id.org/karectl-crates/cr8tor-metamodel
name: cr8tor-metamodel
title: cr8tor-metamodel
description: |-
  Cr8tor metamodel project
license: MIT
see_also:
  - https://karectl-crates.github.io/cr8tor-metamodel

prefixes:
  cr8tor_metamodel: https://w3id.org/karectl-crates/cr8tor-metamodel/
  linkml: https://w3id.org/linkml/
  biolink: https://w3id.org/biolink/
  schema: http://schema.org/
  PATO: http://purl.obolibrary.org/obo/PATO_
  example: https://example.org/
default_prefix: cr8tor_metamodel
default_range: string

imports:
  - governance_model
  - data_model
  - deployment_model

classes:
  cr8tor:
    tree_root: true
    description: The root container class for the Cr8tor metamodel, aggregating all project data including governance, data ingress, and deployment configuration. This class represents the complete state of a Cr8tor project instance.
    attributes:
      governance:
        description: The governance configuration for the project, managing user access, membership, and project metadata. Required.
        range: Governance
        required: true
      ingress:
        description: The data ingress configuration for the project, specifying data sources, destinations, and datasets involved in data movement. Required.
        range: Ingress
        required: true
      deployment:
        description: The deployment configuration for the project, detailing the resources and environment to be provisioned. Required.
        range: Deployment
        required: true